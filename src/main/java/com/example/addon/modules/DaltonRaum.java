/*
//Yes I copied the code from Anticope/meteor-rejects no idc

package com.example.addon.modules;

import com.example.addon.MUtils;
import meteordevelopment.meteorclient.settings.BoolSetting;
import meteordevelopment.meteorclient.settings.SettingGroup;
import meteordevelopment.meteorclient.settings.Setting;
import meteordevelopment.meteorclient.systems.modules.Module;

public class DaltonRaum extends Module {

    private final SettingGroup sgGeneral = settings.getDefaultGroup();

    private final Setting<Boolean> TPCenter = sgGeneral.add(new BoolSetting.Builder())
        .name("TPCenter")
        .description("Whether to use PaperClip to Center yourself")
        .defaultValue(true)
        .build()
    );

    public DaltonRaum() {
        super(MUtils.CATEGORY, "DaltonRaum", "IFYKY");
    }


}
*/
